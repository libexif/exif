SUBDIRS = m4 po libjpeg exif

man_MANS = exif.1

# newer versions of automake/autoconf require this, OK?
EXTRA_DIST = exif.1 exif.spec

# It is not sufficient to have these options somewhere in autogen.sh!
ACLOCAL_AMFLAGS = -I m4
